# Browser-visible configuration only. Provider credentials and provider URLs
# belong in the backend environment and must never be added here.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
